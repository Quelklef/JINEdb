{
  "name": "jinedb",
  "version": "0.3.1",
  "description": "For when Json Is Not Enough",
  "main": "dist/jine.js",
  "types": "dist/jine.d.ts",
  "scripts": {
    "dev": "nodemon --watch src -x \"npm run dev-one\"",
    "dev-one": "tsc --incremental && npm run lint",
    "lint": "eslint --cache --ext .ts src/",
    "test": "npx jest --clearCache && npx jest --watch",
    "test-one": "npx jest --clearCache && npx jest",
    "update-docs": "git checkout gh-pages && git pull origin master --no-edit && npx typedoc && git add . && git add -f docs/ && git commit -m 'update docs' && git push && git checkout master"
  },
  "prepublish": "rm -rf ./dist && tsc",
  "keywords": [],
  "author": "Quelklef",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^25.2.3",
    "@typescript-eslint/eslint-plugin": "^2.34.0",
    "@typescript-eslint/parser": "^2.34.0",
    "core-js-bundle": "^3.6.5",
    "eslint": "^7.0.0",
    "fake-indexeddb": "^3.0.1",
    "jest": "^26.0.1",
    "jest-html-reporter": "^3.1.3",
    "regenerator-runtime": "^0.13.5",
    "ts-jest": "^26.0.0",
    "typedoc": "^0.17.7",
    "typedoc-plugin-pages": "^1.0.1",
    "typescript": "^3.9.2"
  },
  "dependencies": {
    "node": "^14.2.0",
    "nodemon": "^2.0.4",
    "npm": "^6.14.5",
    "true-clone": "^0.5.2"
  }
}
